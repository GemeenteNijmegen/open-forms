info:
  name: Healthcheck metadata
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}$metadata"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
