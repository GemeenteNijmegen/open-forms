info:
  name: 3 (prod) Versie aanmaken
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrlObjecttypes}}/{{objectTypeUuid}}/versions"
  headers:
    - name: Content-Type
      value: application/json
      type: header
  body:
    type: json
    data: |-
      {
        "status": "published",
        "jsonSchema": {}
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
