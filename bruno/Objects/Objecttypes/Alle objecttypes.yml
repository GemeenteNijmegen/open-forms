info:
  name: Alle objecttypes
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrlObjecttypes}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
