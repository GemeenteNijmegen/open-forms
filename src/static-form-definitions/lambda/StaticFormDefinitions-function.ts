// ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".
import * as path from 'path';
import * as lambda from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';

/**
 * Props for StaticFormDefinitionsFunction
 */
export interface StaticFormDefinitionsFunctionProps extends lambda.FunctionOptions {
}

/**
 * An AWS Lambda function which executes src/static-form-definitions/lambda/StaticFormDefinitions.
 */
export class StaticFormDefinitionsFunction extends lambda.Function {
  constructor(scope: Construct, id: string, props?: StaticFormDefinitionsFunctionProps) {
    super(scope, id, {
      description: 'src/static-form-definitions/lambda/StaticFormDefinitions.lambda.ts',
      ...props,
      runtime: new lambda.Runtime('nodejs24.x', lambda.RuntimeFamily.NODEJS),
      handler: 'index.handler',
      code: lambda.Code.fromAsset(path.join(__dirname, '../../../assets/static-form-definitions/lambda/StaticFormDefinitions.lambda')),
    });
    this.addEnvironment('AWS_NODEJS_CONNECTION_REUSE_ENABLED', '1', { removeInEdge: true });
  }
}