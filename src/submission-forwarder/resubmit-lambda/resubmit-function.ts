// ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".
import * as path from 'path';
import * as lambda from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';

/**
 * Props for ResubmitFunction
 */
export interface ResubmitFunctionProps extends lambda.FunctionOptions {
}

/**
 * An AWS Lambda function which executes src/submission-forwarder/resubmit-lambda/resubmit.
 */
export class ResubmitFunction extends lambda.Function {
  constructor(scope: Construct, id: string, props?: ResubmitFunctionProps) {
    super(scope, id, {
      description: 'src/submission-forwarder/resubmit-lambda/resubmit.lambda.ts',
      ...props,
      runtime: new lambda.Runtime('nodejs20.x', lambda.RuntimeFamily.NODEJS),
      handler: 'index.handler',
      code: lambda.Code.fromAsset(path.join(__dirname, '../../../assets/submission-forwarder/resubmit-lambda/resubmit.lambda')),
    });
    this.addEnvironment('AWS_NODEJS_CONNECTION_REUSE_ENABLED', '1', { removeInEdge: true });
  }
}