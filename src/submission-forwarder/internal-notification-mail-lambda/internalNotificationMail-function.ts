// ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".
import * as path from 'path';
import * as lambda from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';

/**
 * Props for InternalNotificationMailFunction
 */
export interface InternalNotificationMailFunctionProps extends lambda.FunctionOptions {
}

/**
 * An AWS Lambda function which executes src/submission-forwarder/internal-notification-mail-lambda/internalNotificationMail.
 */
export class InternalNotificationMailFunction extends lambda.Function {
  constructor(scope: Construct, id: string, props?: InternalNotificationMailFunctionProps) {
    super(scope, id, {
      description: 'src/submission-forwarder/internal-notification-mail-lambda/internalNotificationMail.lambda.ts',
      ...props,
      runtime: new lambda.Runtime('nodejs20.x', lambda.RuntimeFamily.NODEJS),
      handler: 'index.handler',
      code: lambda.Code.fromAsset(path.join(__dirname, '../../../assets/submission-forwarder/internal-notification-mail-lambda/internalNotificationMail.lambda')),
    });
    this.addEnvironment('AWS_NODEJS_CONNECTION_REUSE_ENABLED', '1', { removeInEdge: true });
  }
}