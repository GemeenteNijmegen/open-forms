import { Construct } from 'constructs';

export class SubmissionProcessingMonitor extends Construct {

  // Placeholder for the monitor
  constructor(scope: Construct, id: string) {
    super(scope, id);
  }
}
